name: Deploy to Production

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    environment: production

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma client
        run: npm run db:generate --workspace=@spider/api

      - name: Build applications
        run: npm run build

      # Frontend deployment to Vercel (production)
      - name: Deploy Frontend to Vercel
        uses: amondnet/vercel-action@v25
        with:
          vercel-token: ${{ secrets.VERCEL_TOKEN }}
          vercel-org-id: ${{ secrets.VERCEL_ORG_ID }}
          vercel-project-id: ${{ secrets.VERCEL_PROJECT_ID }}
          vercel-args: '--prod'
          working-directory: ./apps/web
          scope: ${{ secrets.VERCEL_ORG_ID }}

      # Backend deployment (placeholder - configure based on hosting provider)
      - name: Deploy Backend
        run: |
          echo "Backend deployment to production environment"
          # Add your backend deployment steps here
          # This could be Railway, Render, or other hosting provider

      # Run database migrations in production
      - name: Run Database Migrations
        run: |
          echo "Running database migrations in production"
          # npm run db:migrate --workspace=@spider/api
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}